import React from 'react';
import {Button, Link} from 'react-toolbox/lib/button';

export default React.createClass({
  render() {
    return(
      <section className="hero-section" id="story">
        <header>`What's the story?`</header>
        <section className>[CONTENT HERE]</section>
        <footer>
          NEXT
        </footer>
      </section>
    )
  }
});
